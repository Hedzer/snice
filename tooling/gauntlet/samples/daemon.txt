You are writing code for a TypeScript application that uses the Snice web-component framework directly (no React or other UI framework). You have no Snice documentation, source code, examples, skills, or previous attempts. Work only from your existing knowledge and invent APIs when unsure.

Write one complete src/main.ts file for a session-state demonstration. It must define a Snice daemon that owns a user's display name and a revision number. Explicitly create two independent instances of that daemon and make each instance available to a different application root through app context; neither instance may be global or a singleton. A reusable visual element under each root must use request/response communication to read its daemon's current session, use event dispatch/listening communication to change the name and receive change notifications, and visibly render the current name and revision. The consumer element must address its daemon indirectly rather than importing or receiving the daemon implementation class. Include an interactive rename button in each root so independence is observable in a browser.

Output only the exact TypeScript file contents with no Markdown fences, headings, explanation, or other files.
